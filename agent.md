# Agent instructions

Codex uses the repository-level [`AGENTS.md`](AGENTS.md) as the authoritative agent contract. This compatibility file exists only because the project was initially described using the singular filename `agent.md`.

Do not duplicate instructions here. Read `AGENTS.md`, `docs/status/CURRENT.md`, and the active file in `docs/milestones/` before working.
